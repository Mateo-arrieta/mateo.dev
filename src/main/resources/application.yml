server:
    port: 8080
    #error: 
        #include-message: always

spring:
    application:
        name: demo

    datasource: # en produccion seria secrets o variables de entorno
        url: jdbc:mysql://localhost:3306/mateo.dev?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
        username: root
        password: root
        driver-class-name: com.mysql.cj.jdbc.Driver

    jpa:
        hibernate:
            ddl-auto: update #en producion va: validate
        show-sql: true
        properties:
            hibernate:
                format_sql: true
                dialect: org.hibernate.dialect.MySQLDialect

app: 
    jwt: 
        secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970 #agregar un hash
        expiration_ms: 86400000 #agregar un tiempo
